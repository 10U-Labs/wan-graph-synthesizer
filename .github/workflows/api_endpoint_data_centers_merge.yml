---
concurrency:
  cancel-in-progress: false
  group: api-endpoint-data-centers-merge-${{ github.ref }}
jobs:
  post-deployment-integration-tests:
    if: github.ref == 'refs/heads/main'
    name: post-deployment-integration-tests
    needs: reconciliation
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: us-east-2
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - name: Install tools
        run: pip install boto3 boto3-stubs pytest 'python-hcl2<8'
      - name: Run post-deployment integration tests against live AWS
        run: >-
          PYTHONPATH=.:lib/python
          python3 -m pytest
          test/api/endpoints/data-centers/merge/post_deployment/integration/
          --import-mode=importlib --confcutdir=test
          --verbose
  pre-deployment-integration-tests:
    if: github.ref == 'refs/heads/main'
    name: pre-deployment-integration-tests
    needs: unit-tests
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: us-east-2
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - name: Set up OpenTofu
        uses: opentofu/setup-opentofu@v2
        with:
          tofu_wrapper: false
      - name: Install tools
        run: pip install boto3 boto3-stubs pytest 'python-hcl2<8'
      - name: Initialize the stack for state inspection
        run: tofu -chdir=src/api/endpoints/data-centers/merge init -input=false
      - name: Run pre-deployment integration tests
        run: >-
          PYTHONPATH=.:lib/python
          python3 -m pytest
          test/api/endpoints/data-centers/merge/pre_deployment/integration/
          test/lib/python/test_handler_contracts/pre_deployment/integration/
          test/lib/python/test_terraform_config/pre_deployment/integration/
          --import-mode=importlib --confcutdir=test
          --verbose
  reconciliation:
    if: github.ref == 'refs/heads/main'
    name: reconciliation
    needs: pre-deployment-integration-tests
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v5
        with:
          aws-region: us-east-2
          role-to-assume: ${{ vars.OIDC_ROLE_ARN }}
      - name: Set up OpenTofu
        uses: opentofu/setup-opentofu@v2
        with:
          tofu_wrapper: false
      - name: Reconcile the stack to its declared state
        run: |
          tofu -chdir=src/api/endpoints/data-centers/merge init -input=false
          tofu -chdir=src/api/endpoints/data-centers/merge apply -auto-approve -input=false
  static-analysis:
    name: static-analysis
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: |
          pip install \
            assert-no-inline-directives \
            assert-no-linter-config-files \
            assert-one-assert-per-pytest \
            boto3 boto3-stubs botocore-stubs mypy pylint pytest \
            'python-hcl2<8' pyyaml types-PyYAML yamllint
          npm install -g jscpd
      - name: Lint YAML
        run: |
          EMPTY='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY='key-ordering: enable'
          LL='line-length: disable'
          NL='new-line-at-end-of-file: enable'
          TR='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY, $KEY, $LL, $NL, $TR"
          yamllint --strict \
            --config-data \
            "{extends: default, rules: {$RULES}}" \
            .github/workflows/api_endpoint_data_centers_merge.yml
      - name: Assert no inline directives
        run: >-
          assert-no-inline-directives
          --tools pylint,mypy,yamllint
          --verbose
          ${{ github.workspace }}/lib/python
          ${{ github.workspace }}/src/api/endpoints/data-centers/merge/lambdas
          ${{ github.workspace }}/test/api/endpoints/data-centers/merge
          ${{ github.workspace }}/test/lib/python/repo_utils
          ${{ github.workspace }}/test/lib/python/test_fixtures
          ${{ github.workspace }}/test/lib/python/test_handler_contracts
          ${{ github.workspace }}/test/lib/python/test_module_utils
          ${{ github.workspace }}/test/lib/python/test_s3_store_mock
          ${{ github.workspace }}/test/lib/python/test_terraform_config
          ${{ github.workspace }}/test/lib/python/test_terraform_drift
      - name: Assert no linter config files
        run: >-
          assert-no-linter-config-files
          --linters pylint,mypy,yamllint
          --verbose
          ${{ github.workspace }}
      - name: Assert one assert per pytest
        run: >-
          assert-one-assert-per-pytest
          --verbose
          ${{ github.workspace }}/test/api/endpoints/data-centers/merge/conftest.py
          ${{ github.workspace }}/test/api/endpoints/data-centers/merge/pre_deployment
          ${{ github.workspace }}/test/api/endpoints/data-centers/merge/post_deployment
          ${{ github.workspace }}/test/lib/python/repo_utils
          ${{ github.workspace }}/test/lib/python/test_fixtures
          ${{ github.workspace }}/test/lib/python/test_handler_contracts
          ${{ github.workspace }}/test/lib/python/test_module_utils
          ${{ github.workspace }}/test/lib/python/test_s3_store_mock
          ${{ github.workspace }}/test/lib/python/test_terraform_config
          ${{ github.workspace }}/test/lib/python/test_terraform_drift
      # lib/python/test_published_designs reads a published WAN from the API and measures
      # it with the synthesizer's own distance helper, so scripts/ and the synthesizer are
      # both on the path here: pylint reads every import it is asked to lint, and that
      # module imports the API base and the sender from scripts/seed.py.
      - name: Run pylint on source
        run: >-
          PYTHONPATH=lib/python:scripts:src/api/endpoints/data-centers/merge/lambdas:src/api/endpoints/tenants/wan/post/lambdas
          python3 -m pylint
          src/api/endpoints/data-centers/merge/lambdas/handler.py
          lib/python/
          --fail-on=C,R,W --fail-under=10.0
      - name: Run mypy on source
        run: >-
          MYPYPATH=lib/python:src/api/endpoints/data-centers/merge/lambdas
          python3 -m mypy --strict --explicit-package-bases
          --ignore-missing-imports
          src/api/endpoints/data-centers/merge/lambdas/handler.py
          lib/python/
      - name: Detect copy-paste in source
        run: >-
          jscpd --pattern "**/*.py" --threshold 0 --reporters console
          src/api/endpoints/data-centers/merge/lambdas
          lib/python/
      - name: Run pylint on tests
        run: >-
          PYTHONPATH=.:lib/python:src/api/endpoints/data-centers/merge/lambdas
          python3 -m pylint --recursive=y
          test/api/endpoints/data-centers/merge/conftest.py
          test/api/endpoints/data-centers/merge/pre_deployment
          test/api/endpoints/data-centers/merge/post_deployment
          test/lib/python/repo_utils
          test/lib/python/test_fixtures
          test/lib/python/test_handler_contracts
          test/lib/python/test_module_utils
          test/lib/python/test_s3_store_mock
          test/lib/python/test_terraform_config
          test/lib/python/test_terraform_drift
          --fail-on=C,R,W --fail-under=10.0
      - name: Run mypy on tests
        run: >-
          MYPYPATH=lib/python:src/api/endpoints/data-centers/merge/lambdas:test/api/endpoints/data-centers/merge
          python3 -m mypy --strict --explicit-package-bases
          --ignore-missing-imports
          test/api/endpoints/data-centers/merge/conftest.py
          test/api/endpoints/data-centers/merge/pre_deployment
          test/api/endpoints/data-centers/merge/post_deployment
          test/lib/python/repo_utils
          test/lib/python/test_fixtures
          test/lib/python/test_handler_contracts
          test/lib/python/test_module_utils
          test/lib/python/test_s3_store_mock
          test/lib/python/test_terraform_config
          test/lib/python/test_terraform_drift
      - name: Detect copy-paste in tests
        run: >-
          jscpd --pattern "**/*.py" --threshold 0
          --reporters console
          test/api/endpoints/data-centers/merge/
          test/lib/python/repo_utils
          test/lib/python/test_fixtures
          test/lib/python/test_handler_contracts
          test/lib/python/test_module_utils
          test/lib/python/test_s3_store_mock
          test/lib/python/test_terraform_config
          test/lib/python/test_terraform_drift
      - name: Set up OpenTofu
        uses: opentofu/setup-opentofu@v2
        with:
          tofu_wrapper: false
      - name: Validate the stack
        run: |
          tofu -chdir=src/api/endpoints/data-centers/merge init -backend=false -input=false
          tofu -chdir=src/api/endpoints/data-centers/merge validate
  unit-tests:
    name: unit-tests
    needs: static-analysis
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - name: Install tools
        run: pip install boto3 boto3-stubs pytest pytest-cov 'python-hcl2<8' pyyaml
      # The shared test machinery under lib/python/ that this workflow's own tests import,
      # gated one module at a time. A module named in its own --cov fails when it arrives
      # without tests, where one gate over the directory would let its neighbours' numbers
      # carry it (GitHub issue #52).
      - name: Run unit tests with a 100% coverage gate
        run: >-
          PYTHONPATH=.:lib/python:src/api/endpoints/data-centers/merge/lambdas
          python3 -m pytest
          test/api/endpoints/data-centers/merge/pre_deployment/unit/
          test/lib/python/repo_utils/pre_deployment/unit/
          test/lib/python/test_fixtures/pre_deployment/unit/
          test/lib/python/test_handler_contracts/pre_deployment/unit/
          test/lib/python/test_module_utils/pre_deployment/unit/
          test/lib/python/test_s3_store_mock/pre_deployment/unit/
          test/lib/python/test_terraform_config/pre_deployment/unit/
          test/lib/python/test_terraform_drift/pre_deployment/unit/
          --import-mode=importlib --confcutdir=test
          --verbose --pythonwarnings=error
          --cov=src/api/endpoints/data-centers/merge/lambdas
          --cov=lib/python/repo_utils
          --cov=lib/python/test_fixtures
          --cov=lib/python/test_handler_contracts
          --cov=lib/python/test_module_utils
          --cov=lib/python/test_s3_store_mock
          --cov=lib/python/test_terraform_config
          --cov=lib/python/test_terraform_drift
          --cov-branch
          --cov-report=term-missing
          --cov-fail-under=100
name: api_endpoint_data-centers_merge
on:
  push:
    branches: [main]
    paths:
      - src/api/endpoints/data-centers/merge/**
      - test/api/endpoints/data-centers/merge/**
      - lib/python/**
      - lib/opentofu/common/**
      - test/lib/python/repo_utils/**
      - test/lib/python/test_fixtures/**
      - test/lib/python/test_handler_contracts/**
      - test/lib/python/test_module_utils/**
      - test/lib/python/test_s3_store_mock/**
      - test/lib/python/test_terraform_config/**
      - test/lib/python/test_terraform_drift/**
      - .github/workflows/api_endpoint_data_centers_merge.yml
  workflow_dispatch: {}
